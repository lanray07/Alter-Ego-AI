# Optional: base URL for the compatible remote AI backend, including the trailing slash.
# Never put a provider API key in this file or in the Android application.
AI_BACKEND_URL=https://your-backend.example/
